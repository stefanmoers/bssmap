# Für lokalen HTTP-Betrieb mit Docker bleibt dieser Wert false.
# Hinter einem öffentlichen HTTPS-Reverse-Proxy muss er true sein.
BSSMAP_SECURE_COOKIES=false

# Im öffentlichen Betrieb auf die tatsächliche HTTPS-Adresse setzen.
BSSMAP_PUBLIC_ORIGIN=
